import React, {Component} from 'react';

export default class TriggerSettings extends Component {
    render() {
        return (
            <h1>Set the conditions to trigger the alert:</h1>
        );
    }
}